blank_issues_enabled: false
contact_links:
  # Fork setup: repoint this URL to your own docs/SCHEMA.md — contact_links
  # require an absolute URL, so it cannot auto-adapt. See CONTRIBUTING.md#fork-setup.
  - name: Schema reference
    url: https://github.com/vig-os/part-registry/blob/main/docs/SCHEMA.md
    about: Field definitions, enums, ID format, and per-status rules.
  - name: App bugs (part-registry-app)
    url: https://github.com/vig-os/part-registry-app/issues
    about: Report issues with the viewer/editor app, not the registry data.
